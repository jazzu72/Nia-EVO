TWILIO DEBUG REPORT
Generated: $(date)

═════════════════════════════════════════════════════════════════════════════

NETWORK TESTS:
  Twilio.com: $(curl -s -I https://www.twilio.com/console > /dev/null 2>&1 && echo "✅" || echo "❌")
  API.Twilio.com: $(curl -s -I https://api.twilio.com > /dev/null 2>&1 && echo "✅" || echo "❌")

CREDENTIALS CHECK:
  .env file exists: $([ -f .env ] && echo "✅" || echo "❌")
  Account SID length: $(grep "TWILIO_ACCOUNT_SID" .env | cut -d'=' -f2 | wc -c) chars
  Auth Token length: $(grep "TWILIO_AUTH_TOKEN" .env | cut -d'=' -f2 | wc -c) chars
  Phone number format: $(grep "TWILIO_PHONE_NUMBER" .env | cut -d'=' -f2)

═════════════════════════════════════════════════════════════════════════════

