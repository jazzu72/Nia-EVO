services:
  - type: web
    name: nia-capital-os
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node revenue-api.js
    healthCheckPath: /health
    autoDeploy: true
