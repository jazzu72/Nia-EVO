services:
  - type: web
    name: nia-evo-3
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node revenue-api.js
    envVars:
      - key: NODE_ENV
        value: production
