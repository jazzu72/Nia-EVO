services:
  - type: web
    name: nia-capital-os
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node revenue-api.js
    healthCheckPath: /api/revenue
    autoDeploy: true
