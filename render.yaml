services:
  - type: web
    name: nia-evo-production
    runtime: node
    plan: standard
    buildCommand: npm install
    startCommand: node server-watson.js
    healthCheckPath: /api/watson/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000

